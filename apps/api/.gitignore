# apps/api 局部 .gitignore
# 与根 .gitignore 互补,只覆盖 apps/api 目录树
# 注意:此文件中的路径模式相对本文件所在目录(apps/api/),不是 git 根

# 本地 LLM 权重(Qwen2.5-7B GGUF ~4.4GB)不进 git
# README / sha256 sidecar 仍保留在 git
vendor/llm/*.gguf

# Phase 3 Task 5:玩家 .protoforge 作品存档 + 上传队列(运行时数据,不进 git)
# 路径:在 settings.data_dir 下(默认 <project>/.protoforge/data/exports/ 和 /upload_queue/)
# 测试可改 data_dir 到 tmp_path
# 整段 .protoforge/ 在根 .gitignore 已 ignore
exports/
