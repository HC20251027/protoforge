# ProtoForge 开发规范 — pre-commit hooks

> 装一次,跑所有 commit:
> ```bash
> pipx install pre-commit
> pre-commit install
> ```
>
> 手动跑全量:
> ```bash
> pre-commit run --all-files
> ```

## 设计原则

**pre-commit 只挂秒级 hooks**(commit 前 < 1s):
- 通用文件健康(行尾、尾行、大文件)
- Python 静态检查(ruff lint + format)
- Secret 扫描(gitleaks)
- CI workflow 自身合法性(yamllint + actionlint)

**重量级给 CI**:
- pytest 全量(213 个) — CI 跑
- vitest 全量(42 个) — CI 跑
- TypeScript `tsc --noEmit` — CI 跑
- ESLint — CI 跑(本地靠 VSCode 实时提示)

理由:挂到 pre-commit 会让 `git commit` 慢 5-10 秒,被开发者立刻 `git commit --no-verify` 跳过。

## 文件

- `.pre-commit-config.yaml` — 本文件
- `.pre-commit-hooks.yaml` — 不存在(我们不发布 hooks,只消费 hooks)

## 升级 hook 版本

```bash
pre-commit autoupdate
# 跑测试确保 hooks 兼容
pre-commit run --all-files
```

## 跳过某个 hook

只在紧急情况下使用,事后补跑 `pre-commit run --all-files`:
```bash
SKIP=gitleaks git commit -m "..."  # 跳过 secret scan
git commit -m "..." --no-verify    # 跳过所有 hooks
```
